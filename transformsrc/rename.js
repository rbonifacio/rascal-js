const doNotDetect = 7;
example3();

return isCalouro ? "Calouro" : 'Veterano'


function example(variable){
    const detect = 3;
    let list = []
    if(detect){
        console.log(detect);
    }
    for(let x of list){
        // const detect = 3;
    }

    const object_detect = { detect: 7};
    console.log(object_detect.detect);
    console.log(detect);
    

    // this code should print 3 and then 7
}
function example3(){
    const  caio = 2, adriano = 3;
    console.log(caio,adriano)
}

function example2(variable){
    const detect2 = 4;
    if(detect){
        console.log(detect);
    }
    const object_detect2 = { detect: 7};
    const object_detect2 = { detect: 7};
    console.log(object_detect2.detect);

    // this code should print 3 and then 7
}